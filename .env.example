DATABASE_URL="postgresql://postgres:postgres@localhost:5433/bfin_backend_dev"

# Segredo compartilhado com o bfin-app (mesmo padrão do CRON_SECRET) — protege
# as rotas internas do backend. Gere com: openssl rand -base64 32
INTERNAL_API_SECRET="gere-com-openssl-rand-base64-32"
